# restoconnect - to connect every resto services


### To Compile & Run

- docker, docker compose must be in your system; then run the following command:

```bash
docker compose up
```

##### whats included here
 - postgres service



```env
PUBLIC_ENC_KEY=""
DATABASE_URL="postgres://<username>:<password>@localhost/<db_name>"
JWT_SECRET=""
REDIS_URL=""
API_KEY=""
```
